<?php
    // Arithmetic operators
    // + - * / ** %

    $x = 10;
    $y = 2;
    $z = null;

    // $z = $x + $y;
    // $z = $x - $y;
    // $z = $x * $y;
    // $z = $x / $y;
    // $z = $x ** $y;
    // $z = $x % $y;
    echo $z;

    // Increment/Decrement operators
    $counter = 0;

    // https://youtu.be/zZ6vybT1HQs?t=1961